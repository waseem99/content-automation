# Copy to .env inside this folder. Never commit the resulting .env file.
POSTGRES_DB=content_automation_staging
POSTGRES_USER=content_operator
POSTGRES_PASSWORD=REPLACE_WITH_A_LONG_RANDOM_PASSWORD
POSTGRES_PORT=5433

OPERATOR_API_PORT=8000
OPERATOR_KEY=REPLACE_WITH_A_DIFFERENT_LONG_RANDOM_KEY
OPERATOR_ID=waseem-staging

# Exact dashboard origin only. Do not use *.
OPERATOR_CORS_ORIGINS=http://127.0.0.1:3000
