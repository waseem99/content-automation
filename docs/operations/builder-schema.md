# Builder Schema

Stored rows include workflow id, intake id, worker stage id, output hash, reference metadata, draft notes, quote ranges, entities, freshness metadata, citations, confidence note, and provider metadata.

The builder is manual-reference only in P2-02. Later phases can add retrieval adapters after the review gate is in place.
