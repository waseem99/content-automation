[pytest]
markers =
    acceptance: executable acceptance and migration-contract tests
    integration: tests that require PostgreSQL, object storage, queues, or external providers
    compliance: rights, factuality, disclosure, privacy, and publication-gate tests
addopts = -ra
